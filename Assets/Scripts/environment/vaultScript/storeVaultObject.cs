using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class storeVaultObject : MonoBehaviour
{
    // Start is called before the first frame update
    public Transform vaultLandPoint; //stores the point that the player will land at when they vault

}
